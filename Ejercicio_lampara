library ieee;
use ieee.std_logic_1164.all;

entity lampara is
port(
		a:in std_logic;
		b: out std_logic);
end lampara;

architecture arq_lampara of lampara is
			morse: process(a,b) begin
				if a '1' the b <= '1';
				else b <= '0';
				end if;
	end process morse;
end arq_lampara;
